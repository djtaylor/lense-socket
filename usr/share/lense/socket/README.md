### Lense Socket.IO Proxy Shared Folder

When installed Node.js modules using NPM, this folder should be used as the prefix:

```sh
$ sudo npm install --prefix /usr/share/lense/socket socket.io winston
```